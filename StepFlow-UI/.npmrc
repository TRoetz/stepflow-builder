# The locked tree pairs vite@8.2.1 with @vitejs/plugin-react@4.7.0, whose peer
# range stops at vite ^7. Local installs have always used legacy peer resolution;
# keep clean installs (npm ci in Docker) consistent with that.
legacy-peer-deps=true
